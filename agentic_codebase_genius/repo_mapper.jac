# Minimal repo_mapper stubs compatible with jac v0.9.3

can clone_repo with `root entry {
    print("repo_mapper: clone_repo stub");
}

can build_file_tree with `root entry {
    print("repo_mapper: build_file_tree stub");
}

can summarize_readme with `root entry {
    print("repo_mapper: summarize_readme stub");
}

